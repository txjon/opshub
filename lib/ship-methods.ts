// Single source for the PO ship-method options — used by the PO pre-send modal
// (POTab) and the decorator default-ship-method config (decorators page).
export const SHIP_METHODS = ["UPS Ground", "UPS 2-Day", "UPS Next Day", "UPS Next Day Air Saver", "Freight / LTL", "Ocean", "Pick Up", "Vendor's Choice"];
